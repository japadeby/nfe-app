var request = require('request'),
    cheerio = require('cheerio'),
    iconv  = require('iconv-lite'),
    Promise = require('bluebird');

function inventory(products, app){
  return new Promise(function(resolve, reject) {

  });
}
module.exports = inventory;
